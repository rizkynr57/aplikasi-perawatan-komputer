import 'package:flutter/material.dart';

class MenuScreen2 extends StatelessWidget {
  const MenuScreen2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}
